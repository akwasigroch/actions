name: Print
on:
  workflow_dispatch:
  pull_request:
    branches:
        - main

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run script
        run: ./script.sh
    
      - name: Print test.txt
        run: ./script.sh 