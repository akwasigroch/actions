name: Print
on:
  workflow_dispatch:
  pull_request:
    branches:
        - main

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run script
        run: echo "Hello, action 1!"

  action2:
    needs: print
    uses: ./.github/workflows/actin2.yml